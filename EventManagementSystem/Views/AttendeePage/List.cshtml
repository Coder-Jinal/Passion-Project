@model IEnumerable<EventManagementSystem.Models.AttendeeDto>

@{
    ViewData["Title"] = "List of Attendees";
}

<h1 class="page-title">List of Attendees</h1>

<div class="table-container">
    <table class="attendee-table">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.First().FirstName)</th>
                <th>@Html.DisplayNameFor(model => model.First().LastName)</th>
                <th>@Html.DisplayNameFor(model => model.First().Email)</th>
                <th>@Html.DisplayNameFor(model => model.First().Phone)</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.FirstName)</td>
                    <td>@Html.DisplayFor(modelItem => item.LastName)</td>
                    <td>@Html.DisplayFor(modelItem => item.Email)</td>
                    <td>@Html.DisplayFor(modelItem => item.Phone)</td>
                    <td>
                        <a class="btn btn-details" asp-action="Details" asp-route-id="@item.AttendeeId">Details</a> |
                        <a class="btn btn-edit" asp-action="Edit" asp-route-id="@item.AttendeeId">Edit</a> |
                        <a class="btn btn-delete" asp-action="ConfirmDelete" asp-route-id="@item.AttendeeId">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<p>
    <a class="btn btn-new" asp-action="New">Add New Attendee</a>
</p>

<!-- Embedded CSS -->
<style>
    /* Global styles */
    body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f9;
        margin: 0;
        padding: 0;
    }

    .page-title {
        font-size: 2.5rem;
        text-align: center;
        margin-top: 20px;
        color: #333;
    }

    .table-container {
        width: 80%;
        margin: 20px auto;
        background-color: white;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        padding: 20px;
        border-radius: 8px;
    }

    /* Table styles */
    .attendee-table {
        width: 100%;
        border-collapse: collapse;
    }

        .attendee-table th, .attendee-table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        .attendee-table th {
            background-color: #007bff;
            color: white;
        }

        .attendee-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .attendee-table tr:hover {
            background-color: #f1f1f1;
        }

    /* Button styles */
    .btn {
        text-decoration: none;
        padding: 6px 12px;
        margin: 5px;
        border-radius: 4px;
        color: white;
    }

    .btn-details {
        background-color: #28a745;
    }

    .btn-edit {
        background-color: #ffc107;
    }

    .btn-delete {
        background-color: #dc3545;
    }

    .btn-new {
        background-color: #007bff;
        color: white;
        padding: 10px 15px;
        display: inline-block;
        border-radius: 5px;
    }

    /* Button hover effects */
    .btn:hover {
        opacity: 0.8;
    }

    .btn-new:hover {
        background-color: #0056b3;
    }

    .btn-details:hover {
        background-color: #218838;
    }

    .btn-edit:hover {
        background-color: #e0a800;
    }

    .btn-delete:hover {
        background-color: #c82333;
    }

    /* Centering Add New Button */
    p {
        display: flex;
        justify-content: center;
        margin-top: 20px;
        margin-bottom: 4rem; 
    }
</style>
